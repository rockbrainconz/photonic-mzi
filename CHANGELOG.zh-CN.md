# Changelog

[English](CHANGELOG.md) | [简体中文](CHANGELOG.zh-CN.md)

本文件记录值得注意的变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 新增

- 增加独立的实验性 `IncoherentSolarProcessor` 后端，以正负双轨强度完成任意实数
  矩阵乘加，并使用同时参考通道归一化公共模日照波动。
- 分别建模空间不均匀、波长积分权重误差、差分臂失配、光子计数、探测器与参考噪声。
- 增加中英文模型边界文档、可运行示例和 18 项针对性测试。
- 完整记录双轨推导、公共模归一化成立条件、宽带失效条件、无源/可编程硬件形态、
  用电边界、校准流程与实验验收标准。
- 将日光 API 拆为光功率、探测和解码三段，使真实硬件测得的功率可以复用同一解码器。
- 增加中英文日光分支入口文档，同时保留根 README 作为主线 MZI 项目的规范介绍。

## [1.0.1] - 2026-08-24

### 修复

- 转义强度表达式中的竖线，使读出方式对照表在 PyPI 和 GitHub 上完整显示。

## [1.0.0] - 2026-08-24

首个版本：实现 MZI 网格分解、传播、非理想性模型与双语教学动画。

### 修复

- batch 的每个输入样本现在独立采样动态相位抖动，不再整批共享一份“逐样本”噪声。
- `optical_field()` 只返回探测前光场；相干/直接探测分别在正确的检波域加入等效噪声。
- 输出相位屏纳入静态偏置、动态抖动与相移校准，VOA 误差改为每通道固定偏置。
- 严格校验输入形状、有限数值、实矩阵边界、噪声参数与酉矩阵前置条件。
- 仓库示例始终导入同仓库 `src/`，避免环境中的旧安装包遮蔽当前源码。

### 文档

- 项目转移到 `yaoniming3k/photonic-mzi` 后，更新规范仓库地址、CI 徽章、问题追踪和发行链接。
- 将仓库元数据、无语言后缀文档、包元数据、CLI 输出、示例、CI 名称和生成的视觉素材
  统一设为英文默认；中文译本统一使用 `.zh-CN` 后缀。
- README、模型验证说明、贡献指南、变更记录、公开 API、示例和动画全部提供中英文版本。
- 将项目核心目标明确为“验证光处理器执行矩阵乘加的电路级可行性”，并列出编译、传播、读出与非理想性四段验证链。
- 明确 `fab_*` 是可控相移偏置而非分束器制造误差，`drift_*` 是 i.i.d. 敏感度模型。
- 删除无法由模型支持的系统级能耗、延迟、完整探测噪声和真实校准精度结论。
- 将“路径 MZI 数”更正为“空间模式参与数”，旧 API 保留兼容别名。
- 将“神经网络 90% 算力”改为不绑定固定比例的工作负载描述。
- 修正“SVD 唯一分解”：重复或零奇异值时分解不唯一，正交变换也可能包含反射。
- 将同列 MZI 的“互不相干”改为“占用互不重叠的模式”，并明确输入来自同一相干光源。
- 收窄合成分类示例结论，不再泛化成“低精度对推理都够用”。
- 增加 PyPI Trusted Publishing 工作流，校验 Release 标签与包版本，并隔离构建和发布权限。
- 将 README 链接、图片和安装命令调整为可在 PyPI 正确显示的形式。

### 核心实现

- 消元公式使用 `arctan2` 连续覆盖 `x -> 0` 与 `y -> 0` 退化极限，不依赖绝对阈值。
- `phi` 折回物理等价区间 `(-π, π]`。
- 结构化、稀疏、秩亏和极小尺度矩阵均有独立回归测试。

### 新增

- 支持**非方阵**，内部零填充到 `N = max(n_out, n_in)`，多余端口暗置（E1）。
- 支持**批量输入** `(n_in, B)`，实测比逐条循环快约 340 倍（E2）。
- `NoiseModel` 把**静态相移控制偏置**（`fab_*`，可在理想模型内标定）与
  **每样本动态相位抖动**（`drift_*`，静态表无法消除）分开，另含插损、VOA 误差和探测信噪比。
- `calibrate()` / `reset_calibration()`：模拟出厂表征校准。
  实测静态误差 4.2 bit → 48 bit，动态漂移 3.8 bit → 3.8 bit。
- `read_coherent()` / `read_intensity()`：区分相干（零差）探测与直接光电探测，
  后者会丢失符号信息（P2）。
- 奇异值归一化到 `[0, 1]`，增益记在电域 —— VOA 只能衰减不能放大（P1）。
- `report()`、`unitary_error()`、`mesh_depth`、`path_mzi_count()`：
  芯片规格报告与编译回环自检（E6）。
- **逐行代码 + 逐器件光传播的教学动画**（`python -m photonic_mzi`），
  9 个阶段、494 帧，支持暂停与单步。

### 变更

- 就地两行更新替代「构造 N×N 全矩阵再相乘」：编译 O(N⁵)→O(N³)，前向 O(N³)→O(N²)。
  N=128 实测编译快 11.4 倍、前向快 9.1 倍（E4）。
- 随机数走独立的 `np.random.Generator`，不再依赖也不污染全局 `np.random`（E3）。
- 拆成 `mesh`（纯线性代数）/ `processor`（物理模型）/ `animation`（可视化）三层，
  matplotlib 变成可选依赖 `[viz]`。

### 已知限制

- 用 Reck 三角网格而非 Clements 矩形网格。两者 MZI 数量相同，
  但 Clements 深度只有 `N`（Reck 是 `2N-3`），通常更耐受均匀损耗。
- `calibrate()` 不补偿插损造成的通道失配。
- 前向传播仍是逐台 MZI 的 Python 循环，同一列尚未向量化。
- 未建模波长相关性、偏振、非线性与器件间串扰。

[Unreleased]: https://github.com/yaoniming3k/photonic-mzi/compare/v1.0.1...HEAD
[1.0.1]: https://github.com/yaoniming3k/photonic-mzi/compare/v1.0.0...v1.0.1
[1.0.0]: https://github.com/yaoniming3k/photonic-mzi/releases/tag/v1.0.0
